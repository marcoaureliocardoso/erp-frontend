const API_SERVER: string = 'http://localhost:8080';

const API_VERSION: string = 'v1';

export const API_URL: string = `${API_SERVER}/api/${API_VERSION}`;

export const FINANCIAL_API: string = `${API_URL}/financial`;
