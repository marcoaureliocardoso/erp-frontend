import { ContractEventReminderSortableHeaderDirective } from './contract-event-reminder-sortable-header.directive';

describe('ContractEventSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ContractEventReminderSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
