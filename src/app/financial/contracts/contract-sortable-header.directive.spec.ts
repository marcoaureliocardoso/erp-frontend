import { ContractSortableHeaderDirective } from './contract-sortable-header.directive';

describe('ContractSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ContractSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
