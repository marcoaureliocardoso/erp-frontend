import { ContractEventSortableHeaderDirective } from './contract-event-sortable-header.directive';

describe('ContractEventSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ContractEventSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
