import { ProjectSortableHeaderDirective } from './project-sortable-header.directive';

describe('ProjectSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
