import { CourseSortableHeaderDirective } from './course-sortable-header.directive';

describe('CourseSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
