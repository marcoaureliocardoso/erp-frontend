import { BondSortableHeaderDirective } from './bond-sortable-header.directive';

describe('BondSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BondSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
