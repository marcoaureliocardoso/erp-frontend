import { BondEventReminderSortableHeaderDirective } from './bond-event-reminder-sortable-header.directive';

describe('BondEventSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BondEventReminderSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
