import { GrantorSortableHeaderDirective } from './grantor-sortable-header.directive';

describe('GrantorSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new GrantorSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
