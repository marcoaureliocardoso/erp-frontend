import { EmployeeSortableHeaderDirective } from './employee-sortable-header.directive';

describe('EmployeeSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeeSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
